//
  This template loads for the 'tab.friend-detail' state (app.js)
  'friend' is a $scope variable created in the FriendsCtrl controller (controllers.js)
  The FriendsCtrl pulls data from the Friends service (service.js)
  The Friends service returns an array of friend data
ion-view(view-title='订单详情')
  ion-nav-buttons(side="right")
  ion-content.orderDetail
    div.title
      h4(ng-class="{'unfinish':order.orderStatusId!=='F'}")  
        {{order.address}} {{order.parterNo}}
      img.detailImg(ng-repeat="image in order.images" ng-src="{{tranUrl(image)}}" ng-click="viewImages($index)")
      img.detailImg(ng-src='img/add-photo.png' ng-click="uploadImages()")
    div.detail
      p 驳货地址：
        span {{order.address}}
      p 同行编号：
        span {{order.parterNo}}
      p 进仓编号：
        span {{order.warehouseReceptNo}}
      p 件数：
        span {{_.round(order.count)}}
      p 立方：
        span {{_.round(order.volume, 2)}}
      p 吨位：
        span {{_.round(order.weight, 2)}}
      p 订单日期：
        span {{order.orderDt|date:'yyyy-MM-dd'}}
